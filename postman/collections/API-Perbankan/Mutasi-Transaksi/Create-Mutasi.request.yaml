$kind: http-request
name: Create Mutasi
method: POST
url: "{{base_url}}/api/mutasi"
description: Mencatat mutasi transaksi baru (setor/tarik uang). Nominal harus lebih dari 0.
order: 2000
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "no_rekening": "1001112221",
      "id_jenis_transaksi": 1,
      "nominal": 500000
    }
